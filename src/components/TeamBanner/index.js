import './TeamBanner.css'

const TeamBanner = (args) => {
    
    return (
        <section className='.TeamBanner'>
            <h3>{args.nome}</h3>
        </section>
    )
}

export default TeamBanner;